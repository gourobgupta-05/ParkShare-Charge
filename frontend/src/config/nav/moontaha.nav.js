/**
 * Nav links owned by S. Moontaha Rahman [SMR].
 * Add one entry per screen you build: { href, label, roles: [ROLES.DRIVER], owner: 'SMR' }
 */
import { ROLES } from '@/lib/constants';

const links = [];

export default links;
