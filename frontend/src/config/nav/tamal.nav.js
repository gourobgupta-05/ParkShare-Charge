/**
 * Nav links owned by Tamal Deb Nath [TDN].
 * Add one entry per screen you build: { href, label, roles: [ROLES.DRIVER], owner: 'TDN' }
 */
import { ROLES } from '@/lib/constants';

const links = [];

export default links;
