# Dynamic BERC-Based Electricity Tariff Calculator

**Owner:** Gourob Gupta `[GG]` · **Module 3** · **Base path:** `/api/tariff`

Everything for this feature lives in this folder. No other member commits here.
Frontend counterpart: `frontend/src/features/tariff/`

## Rules
- Import from `../../models`, `../../shared/constants`, `../../middleware/auth` — never redefine them.
- Never write a `Booking` field that is not yours (see `models/Booking.js` header).
- Never edit any file marked 🔒.
